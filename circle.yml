machine:
  node:
    version: 6.9.5

dependencies:
  override:
    - yarn:
        pwd: snippet
    - npm install -g elm
  cache_directories:
    - ~/.cache/yarn

test:
  override:
    - yarn run test:
        pwd: snippet
    - elm-make main.elm --yes:
        pwd: client